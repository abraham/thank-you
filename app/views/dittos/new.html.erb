<% # TODO: show avatar of user tweeting %>

<% if @ditto.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@ditto.errors.count, "error") %> prohibited this Thank You from being saved:</h2>

    <ul>
    <% @ditto.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
<%= form_for @ditto, url: thank_dittos_path do |f| %>
  <p>
    <% # TODO: trim text to 138 charactors %>
    <%= f.text_area :text, value: "#{@thank.text} #{thank_url(@thank)}", required: true %>
  </p>

  <p>
    <%= f.submit 'Tweet' %>
    <%= link_to 'Cancel', @thank %>
  </p>
<% end %>
