<p>
  Citations:
  <% links.each do |link| %>
    <span><%= link_to link.text, link.url %><span>
  <% end %>
  <% if current_user == user %>
    <span><%= link_to 'Add link', new_thank_link_url(thank) %></span>
  <% end %>
<p>
